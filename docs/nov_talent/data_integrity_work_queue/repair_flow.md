# Repair Flow

Human Review Queueの修正フローは終了した。現在の操作ステップは0で、画面に修正・Spreadsheet修正済・次へのボタンを表示しない。

過去の17件は終了履歴として固定カテゴリのみ保持し、具体的な人間判断、実氏名、連絡先、候補者値を保存しない。

Data Consistency確認後に新しい修正が必要になった場合は、既存Queueを再開せず、新しいread-only監査結果から別Queueを作成する。
