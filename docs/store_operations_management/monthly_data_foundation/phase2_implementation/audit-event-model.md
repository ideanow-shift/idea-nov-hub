# Audit Event Model

The fixture repository appends immutable audit snapshots for upload, dry-run, validation, import, review, publication, supersession, rollback approval, rollback, and restoration. Events include safe fixture actor reference, version, target period, event type, timestamp, and bounded reason/count metadata.

No raw Workbook content, accounting amounts, personal data, secret, connection detail, or real UUID is retained.
