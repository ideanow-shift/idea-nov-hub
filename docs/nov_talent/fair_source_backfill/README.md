# NOV Talent Fair Source Data Backfill

## 判定

`BLOCKED`。正本Spreadsheetのread-only監査、マッピング契約、dry-run、Snapshot hash生成までは完了しています。Staging反映は0件です。

現行Fair Masterは `participation_fee` と各件数列が `NOT NULL DEFAULT 0` です。正本の空欄は「未登録」であり、0円・0件ではありません。このまま反映すると空欄を0へ誤変換するため、書込みを停止しました。

## 正本

- Spreadsheet: `求人計画27卒_2025年9月～2026年8月`
- Sheet: `ガイダンス/フェア結果`
- Spreadsheet ID: `1nwlOIdQMmPq4ogXOTf-oinAQKnwSTlb3X7Dw8kWowCM`
- Sheet ID: `938747439`
- Access: read-only

個人名・連絡先・参加担当者の実値は、この成果物、GitHub差分、ログへ記録していません。

## 監査結果

| 項目 | 件数 |
|---|---:|
| 取得されたSource行 | 48 |
| 業務値がある行 | 44 |
| 空テンプレート行 | 4 |
| 名称・日付が揃う行 | 36 |
| 名称または日付不足 | 8 |
| 名称・日付の完全一致重複グループ | 0 |
| 数値確認が必要な行 | 1 |
| 費用入力あり | 32 |
| 接触数入力あり | 39 |
| LINE登録数入力あり | 32 |
| 見学取得数入力あり | 33 |

シートのgrid上限は952行（ヘッダー2行を含む）ですが、read-only取得で返った使用範囲は48行です。末尾の空行は実フェア件数へ含めません。

## 既存Fair Masterとの適合

直接対応できるのは、名称、開催日、参加費、担当、接触数、LINE登録数、見学数です。ただし参加費・件数列は未登録値を保持できません。

専用列がない項目は、運営会社、形式、接触見込み数、全体入場数、参加サロン数、備考です。参加担当者はSourceに同名列が2つあり、既存 `assigned_to` は1値だけなので、自動採用しません。

既存Fair Masterは46行（うちactive 45行）です。既存の手入力行は更新していません。名称だけ、または日付だけを根拠に統合しません。

## 再計算ルール

- LINE登録率は `LINE登録数 / 接触数`
- 見学率は `見学取得数 / LINE登録数`
- 採用率と採用単価は、安全にFair IDへ紐付いたSelection Historyだけを使用
- 分母または原数が未登録なら、0%ではなく未登録・集計準備中

## Staging反映前に必要な最小決定

1. Fair Masterの数値列で `NULL=未登録` を保持できるschema変更を承認する。
2. 運営会社、形式、接触見込み、全体入場数、参加サロン数、備考の保持先を正式決定する。
3. 名称・日付不足8行と数値確認1行をHuman Reviewへ送る。
4. 参加担当者の重複ヘッダーをどちらの列から採用するか、または複数担当を別構造にするか決定する。

この決定後に、新規行だけを単一transactionで作成し、既存手入力行は上書きしない形で再dry-runします。
