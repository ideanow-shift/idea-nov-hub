# Validation Rules

Publication-ready fixture output requires 20 stores, Direct 13, FC 7, no duplicate
store ID, one effective mapping per selected P/L sheet, one target month, all eight
required accounts, and finite selected numeric values. Unconfirmed data is not
coerced to zero. Phase 1 does not publish; success is only `DRY_RUN_READY`.
